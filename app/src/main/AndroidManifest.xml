<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.hotel">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".ContactUs">

        </activity>
        <activity android:name=".TripleRoom"
            android:label="Triple Room"></activity>
        <activity android:name=".EconomyRoom"
            android:label="Economy Room"></activity>
        <activity android:name=".StandardRoom"/>
        <activity android:name=".FamilyRoom"/>
        <activity android:name=".roomdetails">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".RoomReservation" />
        <activity android:name=".MainActivity" />
    </application>

</manifest>